{
  "name": "workshops",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:unit:basic": "npx vitest run -r unit/basic --coverage",
    "test:unit:advanced": "npx vitest run -r unit/advanced --coverage",
    "test:mock:basic": "npx vitest run -r mock/basic --coverage",
    "test:mock:advanced": "npx vitest run -r mock/advanced --coverage"
  },
  "keywords": [
    "unit tests",
    "vitest"
  ],
  "author": "eeskov",
  "license": "MIT",
  "dependencies": {
    "@vitest/coverage-istanbul": "^2.1.1",
    "@vitest/coverage-v8": "^2.1.1",
    "vitest": "^2.0.5"
  }
}
